import { ReceptionChrome } from "@/components/layout/ReceptionChrome";

export default function ReceptionLayout({ children }: { children: React.ReactNode }) {
  return <ReceptionChrome>{children}</ReceptionChrome>;
}
